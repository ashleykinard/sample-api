# Path

licenseId:
  name: licenseId
  in: path
  description: The license's unique ID.
  required: true
  schema:
    $ref: ../schemas/licenses.yml#/licenseId

# Query

licenseIdQuery:
  name: licenseId
  in: query
  description: The license's unique ID.
  required: true
  schema:
    $ref: ../schemas/licenses.yml#/licenseId
