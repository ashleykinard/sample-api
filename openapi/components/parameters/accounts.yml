# Path

accountId:
  name: accountId
  in: path
  description: The user account ID.
  required: true
  schema:
    $ref: ../schemas/accounts.yml#/accountId

# Query

accountIdQuery:
  name: accountId
  in: query
  description: The user account ID.
  required: true
  schema:
    $ref: ../schemas/accounts.yml#/accountId